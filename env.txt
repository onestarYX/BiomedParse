1. conda install pytorch torchvision torchaudio pytorch-cuda=12.4 -c pytorch -c nvidia
   conda install mpi4py
2. conda install -c conda-forge gcc_linux-64 gxx_linux-64
   cd $CONDA_PREFIX/bin
   ln -s x86_64-conda-linux-gnu-gcc gcc
   ln -s x86_64-conda-linux-gnu-g++ g++
3. pip install -r assets/requirements/requirements.txt
